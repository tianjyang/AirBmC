import React from 'react';

const ResultItem = (props) => {
  debugger
  return(
    <div>

      <p> you mad eit here! {JSON.stringify(props.listing)}</p>

    </div>
  );
};

export default ResultItem;
